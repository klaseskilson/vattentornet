<div class="container">
  <div class="row">
    <div class="main-content">
      <div class="pane">
        <h1>Vårt sortiment</h1>
        <p>
          På Pub Vattentornet har vi som mål att servera det bästa utav samtliga
          drycker. Oavsett om du är på jakt efter en ale, lager, whisky eller vin
          är vårt mål att du ska hitta en ny favorit hos oss varje gång. Nedan finner
          du allt vi har inne för tillfället.
        </p>
      </div>

      <div class="pane">
        <% @drink_types.each do |drink_type| %>
          <% beers = drink_type.drinks.where(instock: true) %>
          <% if beers.count > 0 %>
            <h3>
              <%= drink_type.name %>, <%= beers.count %> stk
            </h3>
          <% end %>

          <% beers.each do |drink| %>
            <h4>
              <%= link_to drink.name, stock_drink_path(drink_type, drink), title: "Läs mer om drycken" %>
              <small>
                <%= drink.country %>, <%= drink.price.to_i %> kr
              </small>
            </h4>
          <% end %>
        <% end %>
      </div>
    </div>
    <div class="sidebar">
      <%= render 'layouts/sidebar' %>
    </div>
  </div>
</div>
